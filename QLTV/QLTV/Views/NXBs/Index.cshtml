@model IEnumerable<QLTV.Models.NXB>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>        
            @Html.DisplayName("Tên nhà xuất bản")
        </th>
        <th>
            @Html.DisplayName("Địa chỉ")
        </th>
        <th>
            @Html.DisplayName("Số điện thoại")
        </th>
        <th>
            @Html.DisplayName("Số tài khoản")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENNXB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DIACHI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SDT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STK)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MANXB.Replace(" ","")}) |
            @Html.ActionLink("Details", "Details", new { id=item.MANXB.Replace(" ", "") }) 
            @*@Html.ActionLink("Delete", "Delete", new { id=item.MANXB })*@
        </td>
    </tr>
}

</table>
@*@section Scripts{
    <link href="/Content/dataTable/demo_table.css" rel="stylesheet" type="text/css" />
    <script src="/Scripts/jquery.dataTables.1.8.2.js" type="text/javascript"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $("table").dataTable();
        }
    </script>
}*@
